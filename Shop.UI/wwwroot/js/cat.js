function displayNotification(el) {
    // var top = $(el).offset().top;
    // $('.slide-container').css({ 'top': top + $(el).height() + 10 + "px" });
    // $(".slide-container").slideToggle().delay(2000).slideToggle();
    return true;
}